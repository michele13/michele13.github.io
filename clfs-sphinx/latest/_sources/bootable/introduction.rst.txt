6.1. Introduction 
=================

It is time to make the CLFS system bootable. This chapter discusses creating an ``fstab`` file, building a kernel for the new CLFS system, 
and installing the boot loader so that the CLFS system can be selected for booting at startup. 